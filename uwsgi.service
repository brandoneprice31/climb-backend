  [Unit]
  Description=uWSGI instance to serve climb[Service]
  ExecStartPre=-/usr/bin/bash -c 'mkdir -p /run/uwsgi; chown nginx /run/uwsgi'
  ExecStart=/usr/bin/bash -c 'cd /opt/climb-backend; source venv/bin/activate; uwsgi --ini climb.ini'
[Install] WantedBy=multi-user.target
