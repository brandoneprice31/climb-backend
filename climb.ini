[uwsgi]

project = climb
username = deploy
base = /home/deploy

chdir = %(base)/climb-backend
home = %(chdir)/venv
module = backend.wsgi:application

master = true
processes = 1

uid = deploy
socket = :8001

vacuum = true
