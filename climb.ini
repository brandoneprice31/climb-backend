[uwsgi]

project = climb
username = deploy
base = /home/deploy

chdir = %(base)/climb-backend
home = %(chdir)/venv
module = backend.wsgi:application

master = true
processes = 5

uid = deploy
socket = /run/uwsgi/climb.sock
chown-socket = deploy:nginx
chmod-socket = 664

vacuum = true
